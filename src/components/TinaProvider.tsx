// For now, this is a placeholder that doesn't break the build
// Full TinaCMS setup requires authentication tokens from tina.io
export function TinaProviderWrapper({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}